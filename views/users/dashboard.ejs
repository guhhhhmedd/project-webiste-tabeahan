<!DOCTYPE html>
<html lang="id">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard Peserta - Tabeahan Cendekia</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <link href="https://fonts.googleapis.com/css2?family=Bungee&family=Pixelify+Sans:wght@400;700&display=swap"
    rel="stylesheet">

  <style>
    body {
      font-family: "Pixelify Sans", sans-serif;
      background: #fdfaf5;
    }

    .font-bungee {
      font-family: "Bungee", sans-serif;
    }

    .bg-tabeahan-gold {
      background-color: #b8924a;
    }

    .text-tabeahan-gold {
      color: #b8924a;
    }

    .bg-tabeahan-dark {
      background-color: #4a3721;
    }

    .border-tabeahan {
      border-color: #b8924a;
    }

    /* Efek Hover Kartu */
    .card-hover {
      transition: all 0.3s ease;
    }

    .card-hover:hover {
      transform: translateY(-5px);
    }
  </style>
</head>

<body class="bg-[#fdfaf5] min-h-screen">

  <header
    class="bg-tabeahan-dark text-white p-4 shadow-xl border-b-4 border-tabeahan flex justify-between items-center sticky top-0 z-50">
    <div class="flex items-center gap-3">
      <img src="/logo.png" alt="Logo" class="h-10 w-10 object-contain bg-white rounded-full p-1">
      <h2 class="font-bungee text-base md:text-lg tracking-wider text-yellow-400">Tabeahan Cendekia</h2>
    </div>
    <div class="text-right">
      <span class="text-[9px] block text-orange-200 uppercase tracking-tighter">ID Peserta: #<%= user.id %></span>
      <span class="font-bold text-sm uppercase">
        <%= user.username %>
      </span>
    </div>
  </header>

  <main class="p-4 md:p-8 max-w-6xl mx-auto">

    <div class="mb-8">
      <h1 class="text-2xl font-bungee text-tabeahan-dark">Selamat Datang, <%= user.username %>!</h1>
      <p class="text-slate-500 text-sm">Panel kendali ujian dan informasi peringkat kamu ada di sini.</p>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">

      <div class="lg:col-span-1 space-y-6">

        <div class="bg-white p-6 rounded-2xl shadow-sm border-t-4 border-tabeahan card-hover">
          <h3 class="font-bungee text-tabeahan-gold text-md mb-4 border-b pb-2 flex items-center gap-2">
            Profil Saya
          </h3>
          <div class="space-y-4 text-sm">
            <div>
              <label class="text-[10px] text-slate-400 font-bold block">ALAMAT EMAIL</label>
              <p class="text-slate-700 font-medium">
                <%= user.email %>
              </p>
            </div>
            <div>
              <label class="text-[10px] text-slate-400 font-bold block">STATUS AKUN</label>
              <span class="inline-block mt-1 font-bold px-3 py-1 rounded-full text-[10px] 
                <%= user.status === 'LUNAS' ? 'bg-green-100 text-green-700' : 
                    user.status === 'PENDING' ? 'bg-orange-100 text-orange-700' : 'bg-red-100 text-red-600' %>">
                ● <%= user.status %>
              </span>
            </div>
          </div>
          <a href="/logout"
            class="mt-8 block bg-slate-100 hover:bg-red-500 hover:text-white text-slate-500 px-4 py-2 rounded-xl text-xs transition font-bold text-center uppercase tracking-widest">
            Keluar Sistem
          </a>
        </div>

        <div class="bg-tabeahan-dark text-white p-6 rounded-2xl shadow-xl border-b-4 border-tabeahan card-hover">
          <h3 class="font-bungee text-yellow-400 text-md mb-4 flex items-center gap-2">
            🏆 10 Besar Skor
          </h3>
          <div class="space-y-3">
            <% if (rankings && rankings.length> 0) { %>
              <% rankings.forEach((r, index)=> { %>
                <div
                  class="flex justify-between items-center text-xs p-2 rounded-lg <%= r.username === user.username ? 'bg-yellow-400/20 border border-yellow-400/50' : 'bg-white/5' %>">
                  <span class="<%= r.username === user.username ? 'text-yellow-400 font-bold' : 'text-slate-300' %>">
                    <%= index + 1 %>. <%= r.username %>
                  </span>
                  <span class="font-mono font-bold text-yellow-400">
                    <%= r.skor %>
                  </span>
                </div>
                <% }) %>
                  <% } else { %>
                    <div class="text-center py-4">
                      <p class="text-[10px] text-slate-400 italic">Belum ada data peringkat.</p>
                    </div>
                    <% } %>
          </div>
        </div>
      </div>

      <div class="lg:col-span-2 space-y-6">

        <div
          class="bg-white p-8 rounded-3xl shadow-sm border-t-8 border-tabeahan min-h-[400px] flex flex-col justify-center relative overflow-hidden">
          <div class="absolute -right-10 -bottom-10 text-90px opacity-5 select-none">📝</div>

          <h3 class="font-bungee text-tabeahan-gold text-xl mb-6 text-center">Gerbang Utama Ujian</h3>

          <% if (user.status==='LUNAS' ) { %>

            <% if (user.status_ujian==='SELESAI' ) { %>
              <div class="text-center space-y-4">
                <div class="inline-block p-4 bg-yellow-50 rounded-full mb-2">
                  <span class="text-4xl">🎓</span>
                </div>
                <h4 class="text-slate-400 text-xs font-bold uppercase tracking-widest">Skor Akhir Anda</h4>
                <div class="text-8xl font-bungee text-tabeahan-dark drop-shadow-md">
                  <%= user.skor || 0 %>
                </div>
                <div
                  class="inline-flex items-center gap-2 bg-tabeahan-dark text-yellow-400 px-6 py-2 rounded-full font-bungee text-sm shadow-lg">
                  RANKING #<%= myRank %>
                </div>
                <p class="text-xs text-slate-400 mt-6 max-w-xs mx-auto italic underline decoration-yellow-400/30">
                  "Hasil ini sudah final dan tersimpan dalam database perankingan sistem."
                </p>
              </div>

              <% } else { %>
                <div class="text-center max-w-md mx-auto">
                  <div class="p-6 bg-slate-50 rounded-2xl border-2 border-dashed border-slate-200 mb-6">
                    <p class="text-xs text-slate-500 mb-3 font-bold uppercase">Token Akses Ujian:</p>
                    <div class="bg-tabeahan-dark text-white p-5 rounded-xl shadow-inner group">
                      <span
                        class="text-4xl font-bungee tracking-[0.2em] text-yellow-400 group-hover:scale-110 transition-transform inline-block">
                        <%= user.token_ujian %>
                      </span>
                    </div>
                  </div>

                  <form action="/ujian/mulai" method="POST">
                    <input type="hidden" name="token" value="<%= user.token_ujian %>">
                    <div>
                      <form>
                        <!-- ini belum siap mau bikin input token untuk validasi pembayaran -->
                        <input name="token" id="token">
                      </form>
                    </div>
                    <button type="submit"
                      class="w-full bg-tabeahan-gold text-white py-4 rounded-2xl font-bungee text-lg hover:bg-yellow-700 transition-all shadow-[0_5px_0_0_#8b6d37] active:shadow-none active:translate-y-1"
                      onclick="return confirm('Peringatan: Waktu 60 menit akan langsung berjalan setelah Anda klik OK. Siap?')">
                      MASUK RUANG UJIAN
                    </button>
                  </form>
                  <p class="text-[10px] text-slate-400 mt-4 italic">Pastikan koneksi internet stabil sebelum memulai.
                  </p>
                </div>
                <% } %>

                  <% } else if (user.status==='PENDING' ) { %>
                    <div class="text-center py-10">
                      <div class="text-6xl mb-4 animate-bounce">⏳</div>
                      <h4 class="font-bungee text-orange-500 text-lg">Validasi Pembayaran</h4>
                      <p class="text-sm text-slate-600 mt-2 max-w-sm mx-auto">
                        Bukti transfer Anda sudah kami terima. Admin sedang melakukan pengecekan manual. Silakan refresh
                        halaman ini secara berkala.
                      </p>
                    </div>

                    <% } else { %>
                      <div class="space-y-6">
                        <% if (user.status==='DITOLAK' ) { %>
                          <div class="p-4 bg-red-50 border-l-4 border-red-500 rounded-lg flex items-start gap-3">
                            <span class="text-xl">❌</span>
                            <div>
                              <p class="text-red-700 font-bold text-xs uppercase font-bungee">Pembayaran Gagal</p>
                              <p class="text-[10px] text-red-600">Bukti yang Anda kirim sebelumnya tidak terbaca atau
                                tidak sesuai. Mohon upload ulang.</p>
                            </div>
                          </div>
                          <% } %>

                            <div
                              class="bg-yellow-50 p-4 rounded-xl border border-yellow-100 flex items-center justify-between">
                              <div class="text-xs text-slate-700">
                                <p class="font-bold">Biaya Pendaftaran:</p>
                                <p class="text-lg font-bungee text-tabeahan-dark">Rp 50.000</p>
                              </div>
                              <div class="text-right text-[10px] text-slate-500">
                                <p>BCA: 1234-567-890</p>
                                <p>a.n Tabeahan Cendekia</p>
                              </div>
                            </div>

                            <form action="/users/upload-bukti" method="POST" enctype="multipart/form-data"
                              class="space-y-4">
                              <div class="relative group">
                                <label class="text-[10px] font-bold text-slate-400 ml-1 block mb-1">UPLOAD FOTO BUKTI
                                  TRANSFER</label>
                                <input type="file" name="bukti" required
                                  class="block w-full text-xs text-slate-500 file:mr-4 file:py-2 file:px-4 file:rounded-full file:border-0 file:text-[10px] file:font-bungee file:bg-tabeahan-gold file:text-white hover:file:bg-yellow-700 border-2 border-dashed border-slate-200 p-4 rounded-2xl">
                              </div>
                              <button type="submit"
                                class="w-full bg-tabeahan-dark text-yellow-400 py-3 rounded-xl font-bungee text-sm hover:bg-black transition-all shadow-lg">
                                KIRIM KONFIRMASI PEMBAYARAN
                              </button>
                            </form>
                      </div>
                      <% } %>
        </div>
      </div>

    </div>
  </main>

  <footer class="mt-12 p-8 text-center text-slate-400 text-[10px] uppercase tracking-widest border-t border-slate-200">
    &copy; 2026 Tabeahan Cendekia Examination System. All Rights Reserved.
  </footer>

</body>

</html>