<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login CAT - Tabeahan Cendekia</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <style>
        body {
            background-color: #f8f9fa;
        }

        .brand-gold {
            color: #b8860b;
        }

        .bg-brand-gold {
            background-color: #b8860b;
        }

        .bg-brand-navy {
            background-color: #0f172a;
        }
    </style>
</head>

<body class="flex items-center justify-center min-h-screen p-4">

    <div class="bg-white p-8 rounded-2xl shadow-xl w-full max-w-md border-t-4 border-yellow-600">
        <div class="text-center mb-8">
            <img src="/logo.png" alt="Tabeahan Cendekia" class="h-24 mx-auto mb-4">
            <h1 class="text-2xl font-bold text-slate-800">Login Peserta</h1>
            <p class="text-slate-500 text-sm">Sistem Ujian Kompetensi Dasar</p>
        </div>

        <%# Pesan error biasa %>
        <% if (error) { %>
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4 text-center">
                <p><%= error %></p>
            </div>
        <% } %>

        <%# Pesan rate limit + hitung mundur %>
        <% if (locals.rateLimited) { %>
            <div id="rate-limit-box" class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4 text-center">
                <p class="font-bold">Terlalu banyak percobaan login!</p>
                <p>Coba lagi dalam <span id="countdown" class="font-mono font-bold text-lg"></span></p>
            </div>

            <script>
                const resetTime = <%= resetTime %> * 1000;

                function updateCountdown() {
                    const diff = resetTime - Date.now();

                    if (diff <= 0) {
                        document.getElementById("countdown").textContent = "0:00";
                        location.reload();
                        return;
                    }

                    const menit = Math.floor(diff / 60000);
                    const detik = Math.floor((diff % 60000) / 1000).toString().padStart(2, "0");
                    document.getElementById("countdown").textContent = `${menit}:${detik}`;
                }

                updateCountdown();
                setInterval(updateCountdown, 1000);
            </script>
        <% } %>

        <form action="/login" method="POST" class="space-y-6">
            <div>
                <label class="block text-sm font-medium text-slate-700">Username</label>
                <input id="username" name="username" type="text" placeholder="Contoh: jokowi" required
                    class="w-full px-4 py-3 rounded-lg border border-slate-300 focus:ring-2 focus:ring-yellow-600 focus:outline-none transition">
            </div>
            <div>
                <label class="block text-sm font-medium text-slate-700">Password</label>
                <input id="password" name="password" type="password" placeholder="••••••••" required
                    class="w-full px-4 py-3 rounded-lg border border-slate-300 focus:ring-2 focus:ring-yellow-600 focus:outline-none transition">
            </div>

            <button type="submit" <% if (locals.rateLimited) { %> disabled <% } %>
                class="w-full bg-brand-navy text-white font-bold py-3 rounded-lg hover:bg-slate-800 transition duration-300 shadow-lg disabled:opacity-50 disabled:cursor-not-allowed">
                MASUK KE UJIAN
            </button>
        </form>

        <div class="text-center mt-8 space-y-2">
            <p class="text-xs text-slate-500">
                Belum memiliki akun?
                <a href="/register" class="text-blue-800 font-bold hover:underline">regis di sini</a>
            </p>
            <p class="text-xs text-slate-400 italic">&copy; 2026 Tabeahan Cendekia.</p>
        </div>
    </div>

</body>

</html>
