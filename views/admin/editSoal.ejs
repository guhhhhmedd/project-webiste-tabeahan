<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Soal | Tabeahan Cendekia</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;600;800&display=swap');
        body { font-family: 'Plus+Jakarta+Sans', sans-serif; }
    </style>
</head>
<body class="bg-slate-50 p-4 md:p-10 text-slate-900">

    <div class="max-w-4xl mx-auto">
        <div class="flex flex-col md:flex-row md:items-center justify-between mb-8 gap-4">
            <div>
                <a href="/admin/kelola-soal/<%= encodeURIComponent(s.paket) %>" 
                   class="inline-flex items-center text-indigo-600 text-xs font-extrabold uppercase tracking-widest hover:text-indigo-800 transition group mb-2">
                    <span class="mr-2 group-hover:-translate-x-1 transition-transform">←</span> Kembali ke Daftar Soal
                </a>
                <h1 class="text-4xl font-black text-slate-900 tracking-tight">Edit Soal</h1>
                <p class="text-slate-500 font-medium">Memperbarui data soal pada <span class="text-slate-800 font-bold"><%= s.paket %></span></p>
            </div>
            <div class="bg-white px-5 py-3 rounded-2xl shadow-sm border border-slate-200">
                <span class="block text-[10px] font-black text-slate-400 uppercase tracking-widest">ID Database</span>
                <span class="text-lg font-mono font-bold text-indigo-600">#<%= s.id %></span>
            </div>
        </div>

        <div class="bg-white rounded-[2.5rem] shadow-2xl shadow-slate-200/50 border border-slate-100 overflow-hidden">
            <div class="bg-slate-900 p-4 text-center">
                <p class="text-white/60 text-[10px] font-bold uppercase tracking-[0.2em]">Pastikan semua data terisi dengan benar</p>
            </div>
            
            <form action="/admin/updateSoal" method="POST" class="p-6 md:p-10 space-y-8">
                <input type="hidden" name="id" value="<%= s.id %>">
                
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div class="space-y-2">
                        <label class="text-xs font-black text-slate-400 uppercase ml-2 tracking-widest">Kategori Paket</label>
                        <select name="paket" class="w-full border-2 border-slate-100 p-4 rounded-2xl bg-slate-50 focus:border-indigo-500 focus:bg-white focus:ring-4 focus:ring-indigo-500/5 outline-none text-sm font-bold transition-all appearance-none cursor-pointer" required>
                            <option value="Paket SKD/TKD" <%= s.paket === 'Paket SKD/TKD' ? 'selected' : '' %>>Paket SKD/TKD</option>
                            <option value="Paket Akademik Polri" <%= s.paket === 'Paket Akademik Polri' ? 'selected' : '' %>>Paket Akademik Polri</option>
                            <option value="Paket PPPK" <%= s.paket === 'Paket PPPK' ? 'selected' : '' %>>Paket PPPK</option>
                        </select>
                    </div>
                    <div class="space-y-2">
                        <label class="text-xs font-black text-slate-400 uppercase ml-2 tracking-widest">Bidang Materi</label>
                        <input type="text" name="materi" value="<%= s.materi %>" placeholder="Contoh: TWK atau TIU" class="w-full border-2 border-slate-100 p-4 rounded-2xl bg-slate-50 focus:border-indigo-500 focus:bg-white focus:ring-4 focus:ring-indigo-500/5 outline-none text-sm font-bold transition-all" required>
                    </div>
                </div>

                <div class="space-y-2">
                    <label class="text-xs font-black text-slate-400 uppercase ml-2 tracking-widest">Isi Pertanyaan (Soal)</label>
                    <textarea name="soal" class="w-full border-2 border-slate-100 p-5 rounded-3xl h-48 bg-slate-50 focus:border-indigo-500 focus:bg-white focus:ring-4 focus:ring-indigo-500/5 outline-none text-sm font-medium leading-relaxed transition-all" required><%= s.soal %></textarea>
                </div>

                <div class="space-y-4">
                    <label class="text-xs font-black text-slate-400 uppercase ml-2 tracking-widest">Pilihan Jawaban</label>
                    <div class="grid grid-cols-1 gap-4">
                        <% ['a','b','c','d','e'].forEach(letra => { %>
                        <div class="flex items-center gap-4 group">
                            <div class="w-12 h-12 flex flex-none items-center justify-center rounded-2xl bg-slate-100 text-slate-400 font-black text-sm group-focus-within:bg-indigo-600 group-focus-within:text-white group-focus-within:shadow-lg group-focus-within:shadow-indigo-200 transition-all uppercase">
                                <%= letra %>
                            </div>
                            <input type="text" 
                                   name="opsi_<%= letra %>" 
                                   value="<%= s['opsi_' + letra] %>" 
                                   placeholder="Tuliskan jawaban untuk opsi <%= letra.toUpperCase() %>"
                                   class="flex-1 border-2 border-slate-100 p-4 rounded-2xl bg-white focus:border-indigo-500 outline-none text-sm font-semibold transition-all shadow-sm" 
                                   required>
                        </div>
                        <% }) %>
                    </div>
                </div>

                <div class="pt-8 border-t border-slate-100 flex flex-col md:flex-row md:items-center justify-between gap-6">
                    <div class="max-w-xs w-full">
                        <label class="text-xs font-black text-slate-400 uppercase ml-2 tracking-widest">Kunci Jawaban Benar</label>
                        <div class="relative mt-2">
                            <select name="kunci" class="w-full border-2 border-yellow-400 p-4 rounded-2xl bg-yellow-50 font-black text-slate-800 uppercase appearance-none cursor-pointer focus:ring-4 focus:ring-yellow-500/20 outline-none transition-all shadow-md shadow-yellow-100">
                                <% ['a','b','c','d','e'].forEach(opt => { %>
                                    <option value="<%= opt %>" <%= s.kunci === opt ? 'selected' : '' %>>Opsi <%= opt.toUpperCase() %></option>
                                <% }) %>
                            </select>
                            <div class="absolute right-5 top-1/2 -translate-y-1/2 pointer-events-none font-bold text-yellow-600">▼</div>
                        </div>
                    </div>
                    
                    <div class="flex-1 flex justify-end">
                        <button type="submit" class="w-full md:w-auto px-12 py-5 bg-indigo-600 text-white rounded-2xl font-black text-sm hover:bg-indigo-700 hover:-translate-y-1 active:scale-95 transition-all shadow-2xl shadow-indigo-200 uppercase tracking-[0.15em]">
                            Simpan Perubahan
                        </button>
                    </div>
                </div>
            </form>
        </div>

        <p class="mt-10 text-center text-slate-400 text-[10px] font-bold uppercase tracking-widest">
            Tabeahan Cendekia Admin Panel • v2.0
        </p>
    </div>

</body>
</html>